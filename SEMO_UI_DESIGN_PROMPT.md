<!-- Updated: 2026-09-01 -->

# SEMO 통합 UI 디자인 프롬프트

이 문서는 `SEMO_MORE_FEATURE_GUIDE.md`의 제품·기능 계약을 화면 설계와 구현 판단으로 번역한 단일 프롬프트입니다. 기능 계약과 충돌하면 코드, API/DB 계약, `SEMO_MORE_FEATURE_GUIDE.md` 순으로 우선합니다.

```text
SEMO 사용자·관리자 웹 애플리케이션을 아래 7단계 기준으로 감사하고 설계하고 구현하라. 결과물은 정적 시안이 아니라 실제 동호회 운영 작업을 모바일과 PC에서 끝낼 수 있는 Next.js 애플리케이션이어야 한다.

[입력 구분]
- 사용자 요구: 프로젝트명 SEMO에 맞는 삼각형 정체성, PC·모바일 지원, 화면 전반에서 체감되는 풍부한 움직임, 모임 성장 코어와 바깥 삼각의 명확한 역할 분리.
- 확인된 사실: Next.js 16 App Router, React 19, TypeScript strict, Tailwind CSS 4, Motion 12를 사용한다. 사용자 기본색은 #135bec, 관리자 기본색은 #ec5b13이다. 아이디와 Naver/Kakao OAuth 로그인을 지원한다. 서버 상태는 React Query, 인증과 전역 presenter 상태는 Redux, 화면 초안은 local state가 소유한다.
- 설계 가정: “많이 움직인다”는 무한 반복 효과를 모든 요소에 넣는 뜻이 아니다. 짧은 경로 전환, 목록 등장, 상태 변경, 눌림, 포커스, 드래그, 성공 피드백을 촘촘히 연결해 체감 모션 밀도를 높이고 영구 반복은 전역 배경장과 클럽 내부 상세 성장 코어에만 허용한다.

1. 도메인과 제품 목적
SEMO는 일반 SNS나 기능 수집형 포털이 아니라 동호회의 공지, 일정, 투표, 출석, 업무, 회비, 대회, 직책, 결정, 인수인계를 한 흐름에 남겨 임원 교체 뒤에도 운영 맥락이 이어지게 하는 도구다. 모든 화면은 “지금 해야 할 일”, “왜 이 상태인가”, “다음 담당자는 누구인가”를 빠르게 이해시키고 실제 완료 행위로 연결해야 한다. 기능을 활성화하지 않은 모임도 고장 난 화면처럼 보이면 안 된다.

2. 핵심 사용자와 여정
핵심 사용자는 참여 멤버, 기능을 위임받은 멤버, OWNER·ADMIN 운영진이다. 첫 여정은 로그인 → 내 모임 확인 또는 공개 모임 탐색 → 가입/가입 신청 → 클럽 홈 진입이다. 반복 여정은 하단 대표 공간(홈·게시판·일정·더보기·프로필)에서 콘텐츠 확인과 응답을 끝내는 흐름이다. 운영진은 관리자 내비게이션과 More 허브에서 미처리 항목을 확인하고 생성·승인·마감·기록·인수인계를 완료한다. 상세·편집 모달은 URL 복구가 가능해야 하며 모바일에서는 바텀시트, PC에서는 중앙 대화상자 문법을 따른다.

3. 감성·정체성·서사
핵심 인상은 “살아 움직이는 기하학적 운영 원장”이다. 삼각형은 장식이 아니라 함께·운영·이어짐이라는 세 책임의 균형을 뜻하고, 중앙 코어는 모임에 실제로 쌓인 사람·활동·기록을 압축한 데이터 표식이다. 정돈된 신뢰감 위에 짧고 명확한 생동감을 더한다. 사용자 화면은 선명한 파랑, 관리자 화면은 절제된 주황을 상호작용 강조색으로 사용한다. 보라색 그라디언트 중심의 범용 AI 대시보드, 의미 없는 글래스 카드 남발, 모든 섹션의 동일 카드화는 피한다.

4. 시각 시스템과 토큰
- Plus Jakarta Sans와 한국어 시스템 대체 글꼴을 사용한다. 본문은 14px 이상, 보조 텍스트는 12px 이상이다.
- 배경 #f6f7fa, 표면 #ffffff, 전경 #0f172a, 보조 #5b6474, 선 #dce2ea를 기준으로 하고 상태색은 성공·주의·위험·정보에만 사용한다.
- 반경은 control 12px, card 16px, modal 24px, pill 9999px의 네 단계로 제한한다. 조작 영역은 최소 44px이다.
- 간격은 4·8·12·16·20·24·32·48·64px 계열을 우선한다. 페이지 폭은 사용자 52rem, 관리자/대시보드 60rem, 데이터·넓은 관리자 화면 72rem을 기준으로 하며 PC를 모바일 프레임에 가두지 않는다.
- 그림자는 card, floating, modal 세 단계만 기준으로 삼고 현재 테마의 --primary 하나를 상호작용 강조색으로 쓴다.

5. 컴포넌트와 화면 문법
- 앱 경계에는 `SemoBrandMark`, 특정 클럽 데이터에는 `ClubGrowthCoreMark`를 사용하며 서로 바꾸어 쓰지 않는다.
- 공개 탐색·내 모임 목록·공개 소개에는 바깥 성장 삼각을 숨기고 고정 크기 소재 보석, 최근 14일 활동 기반 별형 반짝임 1~5개, 보석 하단에 가로로 놓인 멤버 규모 세모 0~5개를 함께 표시한다. 규모 세모는 `0명=0개`, `1~4명=1개`, `5~19명=2개`, `20~49명=3개`, `50~99명=4개`, `100명 이상=5개`이며 작고 채워진 동일 크기 표식으로 만든다. 정확한 멤버 수는 정보로 병행하되 보석보다 시각적으로 우세하게 만들지 않는다. 목록 코어의 반짝임은 정적으로 표시한다.
- 클럽 홈 내부 필수 성장 패널은 게임 캐릭터 스탯 화면처럼 고정 크기 티어 보석을 전면 주인공으로 두고, 함께·운영·이어짐 성장 삼각을 그 뒤의 배경 레이어로 겹친다. `0/3`에서도 성장 삼각이 사라지지 않도록 실선 시작 삼각을 두고, 다음 티어까지 33·66·100%의 세 점선 목표 삼각과 현재 성장 실루엣을 보석 주변에서 읽을 수 있게 한다. 화면 상태는 `0/3`·`1/3`·`2/3`·`3/3`의 4단계지만 승급 목표는 3개이며, 선의 명도와 면 채움은 티어 보석보다 약하게 유지한다. 각 축의 현재 꼭짓점은 점선 사이를 연속 이동하지 않고 33·66·100%에 도달했을 때 해당 단계 꼭짓점으로 한 번에 이동한다. 보석 소재는 `RAW`·`IRON`·`BRONZE`·`SILVER`·`GOLD`·`PLATINUM`·`DIAMOND` 전용 투명 이미지 자산으로 구분하고, 하단의 작은 가로형 세모 0~5개는 멤버 규모, 별형 반짝임 1~5개는 최근 활동을 표현한다. 상세 화면의 반짝임만 저속으로 교차 점멸하며 저감 모션에서는 정지한다. 큰 성장 삼각은 클럽 내부에서만 보이고 작은 규모 세모는 모든 티어 표식에 보인다. 가장 낮은 축이 현재 단계를 결정하며 티어는 순위나 인기도가 아니다.
- 모든 티어 표식은 표면 전체를 44px 이상의 클릭·탭·키보드 조작 대상으로 만들되 각 카드에 `표시 설명` 배지나 문구를 반복하지 않는다. 평상시에는 보석·세모·반짝임만 보이고 hover·focus-visible에서 테두리와 포커스 링으로 설명 가능 상태를 알린다. 선택 시 현재 티어, 규모 세모, 반짝임, 성장 삼각과 실제 구간 기준을 설명하는 공통 창을 연다. 기존 모달 안에서는 모달을 겹치지 않고 본문 전환으로 설명을 제공한다.
- 헤더는 한 화면 한 제목과 한 개의 우선 행동을 유지한다. 하단 내비게이션은 아이콘과 텍스트를 함께 표시하고 현재 위치를 삼각 상태 표식으로 보여준다.
- 버튼은 행동 동사로 이름 짓고 한 영역의 주요 버튼은 하나만 둔다. 링크는 이동, 버튼은 상태 변경에 사용한다. 아이콘 단독 버튼은 접근성 이름과 44px 영역을 가진다.
- 폼은 영구 라벨, 구체적 도움말, 필드 가까운 오류, 처리 중 문구를 제공한다. 날짜·시간은 공용 popover, 이진 상태는 공용 `SemoSwitch`, 파괴 작업은 공용 confirm을 사용한다.
- 모달은 닫기, Escape, 포커스 이동·복귀, 배경 스크롤 잠금, 모바일 내부 스크롤을 제공한다. 로딩·빈 상태·오류는 같은 정보 구조를 유지하고 오류는 실패 원인과 다음 행동을 함께 제시한다.

6. 상호작용과 모션
- `SemoMotionField`는 삼각 격자, 저속 부유 도형, 테마색 빛, 스크롤 진행을 콘텐츠 뒤에서 제공한다. 정밀 포인터에서는 포인터를 느리게 따라오는 빛으로 깊이를 더하되 클릭을 가로채지 않는다.
- 경로 전환은 opacity와 8px 이내 y 이동, 0.997 수준 scale로 220ms 안에 끝낸다. 목록은 360ms 이내 제한된 stagger로 등장하고 카드 hover는 3px 이내 상승만 허용한다.
- 토글은 thumb spring, 모달은 fade+짧은 pop 또는 sheet 이동, 토스트는 transform/opacity와 scale 진행선을 사용한다. 드래그 정렬은 실제 순서 변경에만 사용한다.
- 대규모 연속 blur·clip-path 애니메이션과 `transition-all`을 피하고 transform과 opacity를 우선한다.
- `MotionConfig reducedMotion="user"`, `useHydrationSafeReducedMotion`, CSS `prefers-reduced-motion`을 유지한다. reduced motion에서는 의미와 상태를 잃지 않은 정적 최종 상태를 즉시 보여준다.

7. 기술 제약·데이터 경계·수용 기준
- 기존 API·권한·기능 활성화·canonical route 계약을 바꾸지 않는다. 성장 코어는 모임 생성 시 생기는 상시 서버 투영이며 프론트에서 점수를 추측하거나 더미 성장값을 만들지 않는다.
- Server/Client Component 경계를 유지하고 Motion 때문에 전체 페이지를 불필요하게 client로 바꾸지 않는다.
- 1440px, 1024px, 390px, 최소 320px에서 가로 넘침 없이 동작해야 한다. sticky 영역, safe-area, 키보드, 독립 스크롤을 확인한다.
- 키보드로 주요 경로와 모달·popover·탭·토글을 사용할 수 있어야 한다. 포커스는 항상 보이고 색만으로 상태를 구분하지 않는다. 모든 조작 영역은 44px 이상이며 장식 아이콘은 보조기술에서 숨긴다.
- 공개 목록은 core-only, 내부 홈은 full growth frame이다. 11px 이하 텍스트, 접근성 이름 없는 아이콘 버튼, 네이티브 date/time, 무명 폼 필드, 작은 조작 영역, `transition-all`, 숨긴 스크롤 단서를 허용하지 않는다.
- 완료 전 `npm run lint`, `npm run verify:auth`, `npm run verify:ui`, `npm run build`를 실행한다. 실제 브라우저에서 로그인·홈·모임 만들기·클럽 홈·게시판·일정·More·관리자 핵심 화면을 PC·모바일·reduced-motion으로 확인하고, API나 인증 환경 때문에 확인하지 못한 화면은 자동 검증과 구분해 기록한다.
```
